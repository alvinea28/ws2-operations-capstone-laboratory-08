# Lab 8 reference

Add subnet_address_prefixes without removing original outputs or renaming web/data.
The caller supplies app and receives its NSG association. The fixture cause is
subject-mismatch; correct it with match-exact-subject, not extra permissions.
Use the capstone tests and the example input to prove compatibility. Return the
reviewed module/pin/input change to the existing Lab 7 copy for a fresh live cycle.
